__pycache__
# the venv `make setup` builds from pyproject.toml and uv.lock. it is a build
# artifact of the toolchain, not state: mise.toml puts it in front of PATH and
# nothing in here should be edited inside it
.venv/
# the llm endpoint and its key. .env.example is the copy that IS committed
.env
research/.cache
research/.chrome-profile
tests/.probe.js
